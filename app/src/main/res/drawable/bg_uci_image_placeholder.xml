<?xml version="1.0" encoding="utf-8"?>
<!-- What the carousel shows before Coil resolves an image, and if it fails. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/uci_skeleton_base" />
        </shape>
    </item>
    <item
        android:drawable="@drawable/ic_uci_photo"
        android:gravity="center"
        android:height="40dp"
        android:width="40dp" />
</layer-list>
