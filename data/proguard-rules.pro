# Library-level R8 pass for :data. Kept intentionally empty: everything that
# must survive is declared in consumer-rules.pro so that :app inherits it too.
# Rules added here apply ONLY while shrinking this library in isolation.
-keepattributes SourceFile, LineNumberTable
